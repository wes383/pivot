# Pivot

One switch: are you doing something useful, or not?

## Development

```bash
npm install
npm run dev        # http://localhost:3000
```

```bash
npm run lint                           # ESLint
npx tsc --noEmit                       # type check
npm run build && npm start             # production build
```
